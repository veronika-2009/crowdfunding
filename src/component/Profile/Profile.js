import React from 'react';
import styles from './Profile.module.css';



const Profile = ({profile,status, updateStatus, isOwner, store}) => {

    return (
        <div className={styles.body}>
        </div>
    );
}

export default Profile;